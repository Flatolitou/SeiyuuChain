{
  "name": "seiyuuchain-root",
  "version": "1.0.0",
  "scripts": {
    "build": "npm install --prefix frontend && npm run build --prefix frontend && npm install --prefix server",
    "start": "node server/index.js"
  },
  "dependencies": {
    "lucide-react": "^1.8.0",
    "socket.io-client": "^4.8.3"
  }
}
